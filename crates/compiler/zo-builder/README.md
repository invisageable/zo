# zo-builder.

> *The `building` phase.*

## about.

The bytecode is passed to the `builder` module, which generates the final executable code or target language including `.py` and `.wat|.wasm`.    

## dirmap.

```
```
