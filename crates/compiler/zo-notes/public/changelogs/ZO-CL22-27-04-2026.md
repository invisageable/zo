# ZO-CL22 — 2026-04-27

## `while cond => expr;` — line form parity with `for`.

```zo
mut i: int = 0;
while i < 5 => i += 1;
showln(i);  // expected: 5
```

printed `1`. Same shape as the recently-fixed
`loop { ... }` bug: the line form parsed but never
looped. `execute_while` only emitted a label and
pushed a branch ctx — the main walk drives the cond
+ LBrace `BranchIfNot` + RBrace close for the **block**
form, but there's no `=>`/Semicolon hook in the main
walk, so the line form ran the body once and fell
through with no back-edge.

### approach.

Mirror `execute_for`'s line-form inline finalize. The
block form keeps the existing main-walk-driven path
(works fine, identical for both for and while).
`execute_while` now scans for `LBrace` vs `FatArrow` at
header depth 0 up front; if it finds a `FatArrow`, it
inline-finalizes:

```text
loop_label:
  <sub-walk cond from idx+1..fat_arrow_idx>
  cond_sir = pop sir_values
  BranchIfNot cond_sir, end_label
  push_scope
  <sub-walk body from fat_arrow_idx+1..=semicolon_idx>
  drain leftover stack
  Jump loop_label
end_label:
  pop_scope
  branch_stack.pop
  skip_until = semicolon_idx + 1
```

The block form is unchanged: scan returns `None`, the
function pushes the branch ctx and returns; the main
walk continues with the cond expression and the LBrace
fires the existing branch-emission path.

### executor.

`zo-executor/src/executor.rs` —
`execute_while(start_idx, end_idx)` now takes the real
indices (was `_start_idx`, `_end_idx`). New
`scan_while_body_marker` walks the header at depth 0,
returns `Some((fat_arrow_idx, semicolon_idx))` for line
form or `None` for block form. Paren/bracket depth
tracking matches the for-loop scanner so a `=>` inside
e.g. `f(a => b)` (hypothetical) wouldn't be mistaken
for the loop-body marker.

### regression.

`programming/99-bottles-of-beer-on-the-wall.zo` golden
re-baked to its full 397-line output (was an `EXPECTED
OUTPUT: DONE` stub from the WIP move). The program
itself uses the **block** form `while count > 0 { ... }`
and was always correct — only the test's golden was
incomplete. Test now exercises the full song through
shipping.

### suite.

1005 / 1005 unit tests, 338 / 338 integration green;
clippy clean.

---

## status of zo's loop constructs (post-CL22).

| construct                        | status |
| :------------------------------- | :----- |
| `while cond { ... }`             | works  |
| `while cond => expr;`            | works  |
| `for i := a..b { ... }`          | works  |
| `for i := a..b => expr;`         | works  |
| `for x := arr { ... }`           | works  |
| `for x := arr => expr;`          | works  |
| `loop { ... }`                   | works  |
| `break` / `continue` in any      | works  |

---

## `readln()` / `read()` — wired end-to-end.

Both were declared as FFI in `std/io.zo` but had no
runtime export and no codegen wiring. Programs that
called them would have dyld-failed at link time.
This is item §1.2 of the stabilization quest;
**shipped**.

### runtime.

New `zo-runtime/src/io.rs`:
- `_zo_io_readln(buf, max_len) -> isize` — single
  `libc::read(0, …)` then truncate the returned len
  at the first `\n`.
- `_zo_io_read(buf, max_len) -> isize` — single
  `libc::read(0, …)`, no truncation. Multi-read
  accumulation deferred (covers piped + line-
  buffered tty input under the buffer size, which
  is the 99% case).
- Negative return is `-errno` so the caller can
  build `Result::Err`.
- 3 Rust unit tests for `truncate_at_newline`
  (no-newline / one-newline / first-newline-wins).

### codegen.

`emit_io_readln` / `emit_io_read` in
`zo-codegen-arm/src/codegen.rs` mirror the
`emit_io_read_file` shape minus the open/close
syscalls. Both share an `emit_io_read_stdin`
helper that:

1. Sets `X0 = buf_ptr`, `X1 = max_len`.
2. `BL` to the runtime helper.
3. Branches on sign — `n >= 0` → `Result::Ok(str)`
   built on stack with `n` as length prefix; `n < 0`
   → `Result::Err(errno)` with `errno = -n`.
4. Returns the `Result` pointer in `dst`.

Buffer size is the existing `READ_FILE_BUF_SIZE`
(4096 bytes). Stack layout matches `read_file`
exactly — `result_base` carries `[tag][field]`,
`scratch_off` saves the signed return, then the
str header + buffer follows.

### compiler.

`RuntimeNeeds::from_sir` in
`zo-compiler/src/compiler.rs` extends its name
filter to flag `concurrency = true` for `readln`
and `read` so `libzo_runtime.dylib` stages next
to the user binary. The `_zo_io_*` exports route
through the existing `ZO_RUNTIME_SYMBOL_PREFIX`
machinery — no new dylib, no new linker
plumbing.

### regression.

End-to-end verified:
- `echo "hello world" | bin` → `hello world` from
  `readln()`.
- `echo "alpha\nbeta\ngamma" | bin` → all 3 lines
  back from `read()`.

The integration test runner has no stdin-pipe
support today, so no `programming/*.zo` golden —
the Rust unit tests cover the truncation logic and
the codegen path is exercised at every build that
uses the symbols.

### suite.

1008 / 1008 unit tests, 338 / 338 integration green;
clippy clean.

---

## `args() -> []str` — process arguments + FFI-array-return fix.

**Two bugs in one quest.** Implementing `args()`
surfaced a pre-existing analyzer crash on any FFI
returning an array type.

### the feature.

`std/io.zo` declares `pub ffi args() -> []str;`. New
runtime helper `_zo_args` (`zo-runtime/src/io.rs`)
walks `std::env::args_os().skip(1)`, allocates a zo
str header per arg via the now-`pub(crate)`
`alloc_str`, and packs the pointers into a heap-
leaked array (`[len:u64][cap:u64][ptr0]...[ptrN]`).
Returns the array pointer — codegen treats it as any
heap `[]str`.

`emit_io_args` is 4 lines: `BL _zo_args; mov dst, X0`.
Dispatch arm in the codegen's call-name match;
`RuntimeNeeds::from_sir` extended to flag `args` for
dylib staging.

### the surfaced bug.

`pub ffi foo() -> []T` (any T) failed analyzer
binding. Repro:
```zo
pub ffi nope() -> []int;
fun main() {
  imu xs: []int = nope();
  showln(xs.len);          // error: xs not found in scope
}
```

Root cause: `execute_ffi`'s return-type parse called
`resolve_type_token(idx)` (single-token), which can't
consume `[ ` `] ` `T`. The return type silently
defaulted to `unit`, the call's result fell through,
the local never registered.

### fix.

`zo-executor/src/executor.rs` — `execute_ffi`'s `Arrow`
arm checks for `Token::LBracket` at the return
position and dispatches to `resolve_array_type`
(which already handles `[]T`, `[N]T`, multi-dim
`[2][3]int`). Falls back to `resolve_type_token` for
non-array returns. Two-line guard, unblocks
arbitrary array-returning FFIs.

### regressions.

End-to-end verified:
- `bin one two three` → `3` + each arg.
- `bin` (no args) → `0`.
- `pub ffi nope() -> []int; imu xs: []int = nope();
  showln(xs.len);` now compiles and runs.

### suite.

1005 / 1005 unit (3 dropped from the truncate-helper
removal during simplify pass), 338 / 338 integration
green; clippy clean.

---

## simplify pass — `args()` follow-ups.

Three wins from the post-feature simplify review:

1. **Memoize `args()` via `OnceLock<usize>`** —
   the doc-comment claimed "reads argv once" but
   the implementation rebuilt + leaked a fresh
   array on every call. A user calling `args()`
   inside a loop would have leaked N arrays.
   Stashed the pointer behind `OnceLock`; first
   call builds and leaks, every subsequent call
   returns the same pointer. The "1000 calls = 1000
   leaks" hole is closed at zero cost on the first
   call.

2. **Drop the `Vec<Vec<u8>>` scaffolding in
   `_zo_args`** — the prior shape allocated `N+1`
   intermediate `Vec<u8>` (one outer, one per arg's
   `to_vec()`) before re-iterating to write into
   the array. `as_encoded_bytes()` is a borrow on
   Unix, so a single pass over `args_os().skip(1)`
   feeds straight into `alloc_str` with no copies.

3. **Trim the padded `# Safety` paragraph** in
   `_zo_args` — 5 lines explaining that the
   function is *not actually unsafe* (only marked
   so by `extern "C-unwind"` requirement) compressed
   to 2 lines.

### the latent twin bug.

The `[]T` FFI return-type fix had a sibling: the
**param-type** parser in `execute_ffi` (line 7296)
was the same shape — `resolve_type_token` only,
no `LBracket` dispatch. So `pub ffi nope(xs:
[]int)` would silently resolve `xs` as `unit` and
codegen would mis-type the param. Mirror-fix
applied: `Token::LBracket` at the param-type
position dispatches to `resolve_array_type`. Both
FFI return and FFI param sites are now consistent
with the regular fun-def parser.

### deferred (tracked in stabilization §5b).

- Extract `alloc_str_array` + shared
  `RUNTIME_ARRAY_HEADER_SIZE` / `RUNTIME_SLOT_SIZE`
  constants in `zo-runtime/src/arr.rs`. Every
  future `[]str` runtime helper currently re-
  derives the `16 + n*8` math.
- Unify the four type-resolve call sites
  (`execute_fun_def` × 2, `execute_ffi` × 2) behind
  one helper that branches on `LBracket` /
  `LParen` / `FnType` / scalar. Three instances of
  the same bug class is the signal.

### suite.

1005 / 1005 unit, 338 / 338 integration green;
clippy clean.
