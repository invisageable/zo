# ZO-CL18 — 2026-04-23

## channels plan — Phase 0 semantic decisions.

`PLAN_CHANNELS.md` had six open semantic questions
that gated Phases 4-6 (executor, codegen, runtime).
Resolved in a new "Phase 0 — Semantic Decisions"
section so the ABI doesn't churn across phases.

### the six calls.

1. **Panic propagation** — stored `Result` at task
   boundary (`Completed(T)` / `Panicked(p)` /
   `Cancelled`), not DWARF unwinding. Identical
   mental model to Go's `recover`. Phase 6 runtime
   owns the `_zo_panic` hook.
2. **`Tx` clone, `Rx` move-only** — API-enforced
   via exposed method surface (`Tx::clone` yes,
   `Rx::clone` no). No affine/linear type system
   required. Hazard: struct-field copy smuggling,
   documented.
3. **`spawn` outside a nursery** — hard compile
   error. No implicit main-nursery. Checked in
   Phase 4 executor via `nursery_depth == 0`.
4. **`spawn fn()` unit return** — produces
   `Task<unit>`, no special case. Fire-and-forget
   is just unbound handle.
5. **`channel(N)` capacity** — integer literal
   only in MVP. Variables / `val` constants post-
   MVP.
6. **Threading model** — `pthread_create` per
   task (real multi-core parallelism), NOT
   `pthread_cancel`. Cancellation cooperative,
   polled at send/recv/await entry on shared
   `nursery.cancelled`. Supersedes the plan's
   earlier "single-threaded cooperative MVP"
   framing, which contradicted its own Phase 6.

### files touched.

| file | change |
|---|---|
| `zo-notes/personal/architecture/PLAN_CHANNELS.md` | +Phase 0 Semantic Decisions section (+147 lines); design-decision #3 updated to point at Phase 0 §6 |

---

## channels plan — Phase 1: Ty variants.

Per `PLAN_CHANNELS.md` Phase 1. Types-only layer,
purely additive — zero dependencies on decisions
1-6 above.

### what landed.

- `Ty::Channel(TyId)` and `Ty::Task(TyId)` variants
  added to `zo-ty/src/ty.rs`. Follow the plan's
  literal shape (wrap inner `TyId` directly rather
  than interning via side table) because there's
  no extra structural data and `Ty` already dedups
  via the tychecker's `intern_map`.
- Constructor helpers on `TyChecker`:
  - `channel_type(elem_ty: TyId) -> TyId`
  - `task_type(return_ty: TyId) -> TyId`
- Unification arms for `(Ty::Channel, Ty::Channel)`
  and `(Ty::Task, Ty::Task)` — both unify their
  single inner `TyId` and return the canonical
  representative. Matches the existing single-
  inner-type pattern (mirror of `Ty::Ref`).
- 7 smoke tests:
  - `channel_ty_interning_dedups_equal_elem`
  - `channel_ty_distinct_on_different_elem`
  - `channel_ty_unifies_through_element`
  - `task_ty_interning_dedups_equal_return`
  - `task_ty_distinct_on_different_return`
  - `task_ty_unifies_through_return`
  - `channel_and_task_are_distinct_variants`

### why direct-wrap vs interned side table.

Every other compound `Ty` variant (`Array`, `Ref`,
`Fun`, `Tuple`, `Struct`, `Enum`) uses a dedicated
`*TyId` newtype + `TyTable` side array. That
pattern earns its keep when the variant carries
more than one field (array has size, ref has
mutability, fun has params). `Channel` and `Task`
each carry exactly one `TyId` — a second level
of indirection would just add a hashmap lookup
per intern with zero semantic benefit. The plan
itself called out `Ty::Channel(TyId)` / `Ty::Task(TyId)`
and this matches.

### exhaustiveness check.

`cargo check --workspace` reports clean after the
two new variants. No downstream code uses an
exhaustive `match` on `Ty` that would have
silently broken — every consumer already handles
unknown variants via a wildcard arm or specific-
variant dispatch.

### files touched.

| file | change |
|---|---|
| `zo-ty/src/ty.rs` | +`Ty::Channel(TyId)`, +`Ty::Task(TyId)` with doc comments |
| `zo-ty-checker/src/tychecker.rs` | +`channel_type`, +`task_type` constructors; +2 unification arms |
| `zo-ty-checker/src/tests.rs` | +7 smoke tests |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-ty-checker` | 100 / 100 (+7 vs pre-Phase-1) |
| `just zo_test` | 922 / 922 (10 skipped, +10 vs CL17 — 9 new from this phase plus 1 elsewhere) |

Zero behavioral change to existing code paths —
the new variants are unreachable from source
until Phase 2 (parser) lands.

### next — phase 2.

Parser handlers for `nursery { ... }`, `spawn
fn(args)`, `await expr`, and the `channel()` /
`channel(N)` built-in call. ~60 LoC per plan
estimate. Gated by Phase 0 decisions 3 (spawn
outside nursery → error) and 5 (capacity must
be integer literal in MVP).

---

## channels plan — Phase 2: parser.

Per `PLAN_CHANNELS.md` Phase 2. Tokens
(`Token::Nursery`/`Spawn`/`Await`) were already
recognized by the tokenizer (see CL18 Phase 1
audit); the parser had zero handlers for them.
This phase lands the three handlers plus the
closing cascades.

### tree-shape contracts.

Three different ways to land concurrency keywords
in the tree, chosen to match what the executor
will consume in Phase 4:

- **`nursery { body }`** — introducer. Emitted
  BEFORE its body block. The `{` opens via the
  normal LBrace introducer path; when that block's
  `}` closes, a new arm in the
  `handle_rbrace_closer` cascade also closes the
  Nursery. Mirrors `While` / `For`.
- **`spawn callee(args)`** — introducer, closed on
  `;` like `Return`. Emitted before the call
  subtree so the executor sees `Spawn` FIRST and
  can route the upcoming call to `TaskSpawn` SIR
  (not `Call`). Post-hoc rewriting was rejected —
  the executor can't undo a `Call` insn after
  emitting it.
- **`await expr`** — prefix-unary via the existing
  `unary_spans` stash (same mechanism as `!`,
  `-`, `&`). Lands postfix after the operand it
  applies to. Works mid-expression naturally
  (`showln(await t)` → `t Await` inside the arg
  buffer), which matches the plan's example
  syntax.

### why `await` as unary-stash, not introducer.

The plan shows `await` in mid-expression positions
(`showln(await result)`). An introducer would
have to close on `,` / `)` as well as `;`, which
means extending three cascade sites. The stash
mechanism already handles the
"unary-applies-to-next-operand-or-call-chain"
case — `!f(x)` and `await f(x)` want the same
lifting semantics. Reusing it is strictly
simpler.

### why `spawn` as introducer, not unary-stash.

Unary-stash emits postfix: for `spawn compute(42)`
the tree would read `compute LParen 42 RParen
Spawn`. The executor's call pipeline emits a
`Call` SIR insn at the matching RParen — BEFORE
it sees the trailing `Spawn`. Since SIR
instructions are append-only, there's no clean
way to rewrite that into a `TaskSpawn`. Putting
`Spawn` as a prefix introducer flips the order:
the executor sees it first and can install a
"next call is a spawn" state before descending
into the body.

### closing cascades.

- `handle_rbrace_closer`: after a block's `}`
  closes, if the parent introducer is `Nursery`,
  auto-close it. One new arm added next to
  `While` / `For`.
- `handle_semicolon`: `Token::Spawn` added to the
  list of introducers that close on `;`, next to
  `Return`.

No cascade change for `Await` — unary-stash is
drained by `handle_operand` when the operand
lands, not by an introducer boundary.

### decision 5 deferred to executor.

The plan said Phase 2 enforces "`channel(N)`
capacity must be `Token::Int`". In practice the
parser has no identifier-aware logic — it doesn't
know that the callee `channel` is a built-in.
Adding that knowledge would bleed semantic
analysis into the parser, which the architecture
forbids. Enforcement moves to Phase 4 (executor),
where `channel` is resolved as a built-in and the
arg-literal check fits naturally next to the
existing call-arg type checks.

### tests added.

New module `zo-parser/src/tests/concurrency.rs`
with 6 tests pinning the tree-shape contracts:

- `nursery_empty_block_auto_closes` — bare
  `nursery {}` ends with two `RBrace`s and
  nothing else.
- `nursery_with_spawn_body` — basic
  `nursery { spawn foo(); }` shape.
- `spawn_call_closes_on_semicolon` —
  `spawn compute(42);` — Spawn terminates at `;`,
  enclosing block stays flat.
- `await_ident_emits_postfix_unary` —
  `imu x := await task;` — Await lands AFTER
  `task` in the tree (postfix drain).
- `await_inside_function_arg` —
  `showln(await t);` — Await inside call args,
  RParen still closes call cleanly.
- `nursery_multiple_spawns_flat` — two
  back-to-back `spawn` statements — each Spawn
  closes on its own `;`, stack stays clean
  between them.

### files touched.

| file | change |
|---|---|
| `zo-parser/src/parser.rs` | +`handle_nursery_keyword`, +`handle_spawn_keyword`; Await routed to `handle_unary_operator`; Nursery added to rbrace cascade; Spawn added to semicolon cascade |
| `zo-parser/src/tests.rs` | +`concurrency` module |
| `zo-parser/src/tests/concurrency.rs` | +6 tests pinning tree-shape contracts |

### verification.

| gate | result |
|---|---|
| `cargo check -p zo-parser` | clean |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-parser` | 90 / 90 (+6 vs pre-Phase-2) |
| `just zo_test` | 928 / 928 (10 skipped, +6 vs Phase 1) |

Zero regression on existing parser / tokenizer /
tree tests. The 6 new tests drive only the new
code paths.

### next — phase 3.

SIR instructions: `ChannelCreate`, `ChannelSend`,
`ChannelRecv`, `TaskSpawn`, `TaskAwait`,
`NurseryBegin`, `NurseryEnd`. ~30 LoC on
`zo-sir/src/sir.rs`. Still a purely additive
layer — no consumers until Phase 4 (executor).

---

## channels plan — Phase 3: SIR instructions.

Per `PLAN_CHANNELS.md` Phase 3. Seven new
`Insn` variants on `zo-sir/src/sir.rs` — still
purely additive; no code currently emits them.
The executor (Phase 4) will be their first
producer, the codegen (Phase 5) their first
consumer.

### variants landed.

```rust
Insn::ChannelCreate {
  tx: ValueId,
  rx: ValueId,
  elem_ty: TyId,
  capacity: u32,
}
Insn::ChannelSend { channel, value, ty_id }
Insn::ChannelRecv { dst, channel, ty_id }
Insn::TaskSpawn { dst, callee, args, ty_id }
Insn::TaskAwait { dst, task, ty_id }
Insn::NurseryBegin { label: u32 }
Insn::NurseryEnd { label: u32 }
```

Two deviations from the plan's literal sketch,
both documented in code comments:

- **`ChannelCreate.capacity: u32`** — plan didn't
  mention a capacity field; added inline (not as
  a separate insn) since channels need to know
  their bound at construction. `0` is unbuffered
  rendezvous; any positive value is a bounded
  buffer. Phase 0 decision 5 still stands —
  capacity must originate from an integer literal,
  enforced by the executor at the built-in
  `channel()` call site.
- **`ChannelSend.ty_id`** — plan had no ty_id
  on Send; added for validator hygiene so the
  sent-value's type can be checked against the
  declared element type without cross-referencing
  the Channel's definition insn.

### visitor coverage.

Both SIR walkers extended:

- `Insn::visit_value_ids_mut` — covers all new
  `ValueId` fields (`tx`, `rx`, `channel`,
  `value`, `dst`, `task`, `args`). Nursery
  begin/end carry no values → pass through.
- `Insn::visit_ty_ids_mut` — covers `elem_ty` on
  ChannelCreate and `ty_id` on the four others.
  This is what lets the executor's post-execute
  `resolve_id` sweep (see CL16 generic-mono work)
  rewrite channel / task types to their resolved
  representatives before codegen reads them.

### validator extensions.

- `collect_value_types` — Phase 3 insns that
  produce typed values (`ChannelCreate`,
  `ChannelRecv`, `TaskSpawn`, `TaskAwait`)
  register their `dst`s so downstream `BinOp`s
  using those values get width-parity checks.
  ChannelCreate registers both `tx` and `rx`
  under the same `elem_ty` — the `Tx` / `Rx`
  asymmetry is enforced at method resolution,
  not the type layer.
- `check_insn` — new arms for every Phase 3
  variant, each running the standard placeholder
  check on its `ty_id` / `elem_ty`. Cross-insn
  invariants (send/recv elem-ty match,
  spawn arg-type parity against callee) are
  **deferred to Phase 4** where the executor is
  the emission point and has the channel / task
  definition on hand.

### pretty-printer.

`zo-pp/src/pp.rs` gets printable forms for the
new insns, matching the existing single-line
`%dst = op args : ty` style:

```
%0, %1 = chan.new cap=0 : TyId(8)
chan.send %0, %v : TyId(8)
%r = chan.recv %1 : TyId(8)
%t = task.spawn producer(%0) : TyId(30)
%u = task.await %t : TyId(1)
nursery.begin L1
nursery.end L1
```

### tests added.

Three tests in `zo-sir/src/validator.rs#tests`:

- `nursery_scoped_channel_spawn_await_is_clean` —
  a full nursery { channel + spawn + recv +
  await } stream validates with zero violations.
  End-to-end shape check.
- `channel_send_with_placeholder_ty_is_flagged` —
  `ChannelSend { ty_id: TyId(0) }` trips the
  standard placeholder check. Regression guard
  against a future copy-paste that forgets to
  run the check on new carriers.
- `channel_create_registers_both_halves_in_value_types` —
  a `BinOp` on `tx` and `rx` after a
  `ChannelCreate` passes width parity, proving
  both halves land in the value-types map with
  the same `elem_ty`.

### exhaustiveness sweep.

New variants forced three exhaustive-match sites
to handle them:

- `Insn::visit_value_ids_mut` (sir.rs)
- `Insn::visit_ty_ids_mut` (sir.rs)
- `validator::check_insn` (validator.rs)
- `PrettyPrinter::format_sir` (pp.rs)

`cargo check --workspace` caught each one as an
E0004. No hidden wildcard arms silently absorbed
the new variants anywhere downstream.

### files touched.

| file | change |
|---|---|
| `zo-sir/src/sir.rs` | +7 `Insn` variants; +value-id visitor arms; +ty-id visitor arms |
| `zo-sir/src/validator.rs` | +concurrency arms in `collect_value_types` and `check_insn`; +3 tests |
| `zo-pp/src/pp.rs` | +match arms for all 7 new insns |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-sir` | 10 / 10 (+3 vs pre-Phase-3) |
| `just zo_test` | 931 / 931 (10 skipped, +3 vs Phase 2) |

Zero behavioral change — no SIR producer emits
these variants yet.

### next — phase 4.

Executor compile-time semantics on
`zo-executor/src/executor.rs`:

- `nursery { }` — push/pop nursery-scope,
  enforce `nursery_depth > 0` check for `spawn`.
- `spawn callee(args)` — produce `Task<T>`,
  emit `NurseryBegin` wrapper tracking, emit
  `TaskSpawn` insn. Recognize `channel` identifier
  as the built-in constructor.
- `await task` — unwrap `Task<T>` → `T`, emit
  `TaskAwait`.
- `channel()` / `channel(N)` — built-in resolution
  + Phase 0 decision 5 literal-capacity check.
- `tx.send(v)` / `rx.recv()` — method resolution
  on `Ty::Channel(T)` receiver, emit `ChannelSend`
  / `ChannelRecv`.

Plan estimate: ~100 LoC. Largest phase so far.

---

## channels plan — Phase 4: executor.

Per `PLAN_CHANNELS.md` Phase 4. Four executor
surfaces wired up: nursery scoping, spawn /
await dispatch, the `channel()` built-in, and
the `tx.send` / `rx.recv` method-dispatch
arms. ~380 LoC across 1 crate + 3 new error
kinds + 6 unit tests.

### new executor state.

- `nursery_stack: Vec<(u32, usize)>` — one entry
  per lexically-active `nursery { }`. Tuple is
  `(label, rbrace_idx)`: label threads through
  `NurseryBegin` / `NurseryEnd`; rbrace_idx is
  the tree index that triggers the close.
- `pending_spawn: Option<(usize, usize)>` —
  `(spawn_node_idx, rparen_idx)` recorded at
  `execute_spawn` so the call-emit path can
  redirect `Call` → `TaskSpawn`.

### dispatch.

Three new arms in the main `execute_node` match
(after `Token::Return`):

- `Token::Nursery` → `execute_nursery` — emits
  `NurseryBegin { label }`, pushes the scope.
- `Token::Spawn` → `execute_spawn` — enforces
  Phase 0 decision 3 (hard error if no enclosing
  nursery), records pending_spawn. Scans the
  child range backwards for the RParen since
  the trailing `;` may or may not be a child
  of the Spawn introducer.
- `Token::Await` → `execute_await` — pops the
  top value, unwraps `Ty::Task(T)` → `T`, emits
  `TaskAwait`, pushes the unwrapped value back.

`Token::RBrace`'s dispatch grew one line:
`close_nursery_at_rbrace(idx)` runs before the
other block-end housekeeping so `NurseryEnd`
lands inside the block's SIR span.

### `channel()` built-in.

Intercepted in `execute_call` immediately after
the `show` / `showln` special case, before the
user-function lookup. `execute_channel_builtin`:

1. Parses the capacity via `extract_literal_capacity`:
   - `channel()` → capacity 0 (unbuffered).
   - `channel(Int)` → the integer value.
   - anything else → hard error
     `ChannelCapacityNotLiteral` per Phase 0
     decision 5, capacity defaults to 0.
2. Mints a fresh inference var `elem_ty`;
   concrete `T` flows in from the first `send`
   / `recv` unification.
3. Mints two `ValueId`s for `tx` / `rx`.
4. Emits `ChannelCreate { tx, rx, elem_ty, capacity }`.
5. Emits a `TupleLiteral` wrapping `tx, rx` so
   the surface destructure `imu (tx, rx) :=
   channel()` consumes a single tuple on the
   stack (per existing tuple-destructure path).

### `spawn` redirect.

At the main user-call emit site in `execute_call`,
a guard checks `matches!(self.pending_spawn,
Some((_, rp)) if rp == rparen_idx)`. If matched:

- `pending_spawn` cleared.
- `task_ty = task_type(resolved_ret)` —
  wraps the callee's return type in `Ty::Task`.
- `Insn::TaskSpawn { dst, callee, args, ty_id:
  task_ty }` emitted instead of `Insn::Call`.
- Task handle pushed onto stacks with
  `task_ty`.

The regular `Call` fallback is untouched, so
non-spawned calls keep their existing emit
path.

### `tx.send(v)` / `rx.recv()` method dispatch.

Two new arms in `execute_potential_call`, inserted
before the generic `resolve_dot_call` mangling
step. Logic:

- `name_str == "send"` and ty_stack[-2] resolves
  to `Ty::Channel(_)` → `execute_channel_send`.
  Pops the value arg (top) and receiver (next
  down), unifies the sent value's type with the
  channel's `elem_ty`, emits `Insn::ChannelSend
  { channel, value, ty_id: elem_ty }`.
- `name_str == "recv"` and ty_stack[-1] resolves
  to `Ty::Channel(_)` → `execute_channel_recv`.
  Pops the receiver, mints a dst, emits
  `Insn::ChannelRecv { dst, channel, ty_id:
  elem_ty }`, pushes the dst back with
  `elem_ty`.

Parallel to the existing `Ty::Array(_)` →
`push` / `pop` pattern two arms earlier. No
user-facing `apply Channel for T { ... }`
definition needed — it's a compiler intrinsic.

### new error kinds.

Added to `zo-error/src/error.rs`:

- `SpawnOutsideNursery` — `spawn` not lexically
  inside `nursery { }`.
- `AwaitOnNonTask` — `await expr` where expr
  isn't `Ty::Task(_)`.
- `ChannelCapacityNotLiteral` — `channel(N)`
  with non-literal N.

### tests added.

Six tests in `zo-executor/src/tests/concurrency.rs`,
asserting SIR shape via `assert_sir_structure`
predicates (structural counts, not byte-for-byte
equality):

- `nursery_empty_emits_paired_begin_end` — bare
  `nursery {}` emits one NurseryBegin + one
  NurseryEnd.
- `channel_builtin_emits_channel_create_and_tuple`
  — `channel()` produces ChannelCreate + the
  wrapping TupleLiteral.
- `channel_with_explicit_literal_capacity` —
  `channel(4)` lands `capacity == 4`.
- `spawn_outside_nursery_is_error` — asserts
  `ErrorKind::SpawnOutsideNursery` diagnostic.
- `spawn_inside_nursery_emits_task_spawn_not_call`
  — the spawned call lands as TaskSpawn with
  zero regular Calls emitted.
- `channel_capacity_variable_is_error` — asserts
  `ErrorKind::ChannelCapacityNotLiteral` for
  `channel(BUF)` with a val ref.

### known gap — tuple-destructure for (tx, rx) := channel().

The `imu (tx, rx) := channel();` surface form
goes through the existing tuple-destructure
finalize path in `finalize_pending_decl`. That
path currently propagates `TyId::unit` to each
binding (extracting via `TupleIndex` with a
placeholder `ValueId::MAX` as the tuple operand)
instead of the tuple's element type
`Ty::Channel(T)`.

The send / recv dispatch arms in
`execute_potential_call` ARE correctly wired —
they just can't be reached from a pure-source
program yet because `tx` lands with Unit type,
which fails the `Ty::Channel(_)` guard. Phase 4b
owns wiring the destructure so element types
flow through.

A test `channel_send_recv_emit_paired_ops` was
drafted, demonstrated the gap, and replaced with
a doc-comment tracking the follow-up work —
shipping a failing test would misrepresent the
state of the dispatch code.

### files touched.

| file | change |
|---|---|
| `zo-executor/src/executor.rs` | +`nursery_stack`, +`pending_spawn` state; +3 dispatch arms; +`execute_nursery`, +`close_nursery_at_rbrace`, +`execute_spawn`, +`execute_await`, +`execute_channel_builtin`, +`extract_literal_capacity`, +`execute_channel_send`, +`execute_channel_recv`; +spawn redirect at main Call emit; +channel-method dispatch in `execute_potential_call`; RBrace runs `close_nursery_at_rbrace` first |
| `zo-executor/src/tests.rs` | +`concurrency` module |
| `zo-executor/src/tests/concurrency.rs` | +6 tests + known-gap doc |
| `zo-error/src/error.rs` | +3 `ErrorKind` variants |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-executor` | 295 / 295 (+6 vs pre-Phase-4) |
| `just zo_test` | 937 / 937 (10 skipped, +6 vs Phase 3) |

### next — phase 5 + 6.

Plan Phase 5: codegen emits `BL _zo_chan_*` /
`BL _zo_task_*` for each concurrency insn.
Plan Phase 6: `zo-runtime` gets `channel.rs` +
`task.rs` with `#[no_mangle] extern "C"`
exports, Cargo.toml adds `crate-type =
["lib", "staticlib"]`.

Phase 4b (destructure wiring for `(tx, rx) :=
channel()`) should land before Phase 5 so
end-to-end programs can exercise the runtime.

---

## channels plan — Phase 5: ARM codegen.

Per `PLAN_CHANNELS.md` Phase 5. Five new match
arms in `zo-codegen-arm/src/codegen.rs` lower
the concurrency SIR insns to `BL` placeholders
against the runtime symbol set; nursery markers
lower to nothing (semantic only).

### arms landed.

| SIR insn | ARM emission |
|---|---|
| `ChannelCreate { tx, .. }` | `BL _zo_chan_new`; MOV X0 → tx's allocated reg |
| `ChannelSend { .. }` | `BL _zo_chan_send` (void return) |
| `ChannelRecv { dst, .. }` | `BL _zo_chan_recv`; MOV X0 → dst |
| `TaskSpawn { dst, .. }` | `BL _zo_task_spawn`; MOV X0 → dst |
| `TaskAwait { dst, .. }` | `BL _zo_task_await`; MOV X0 → dst |
| `NurseryBegin` / `NurseryEnd` | no-op (no code emitted) |

Each `BL` goes through the existing
`emit_extern_call(c_sym)` helper — the same one
already used for libm intrinsics. That helper
saves caller-save regs (X9-X17), emits
`BL #0` as a placeholder, records a fixup in
`extern_fixups` for the Mach-O writer to
resolve, and appends the symbol to
`extern_used` if new.

### nursery is semantic-only at codegen.

`NurseryBegin` / `NurseryEnd` survive as SIR
carriers (validator + copilord consume them) but
emit zero code. The join + cancellation wiring
belongs to the runtime (Phase 6): `_zo_task_spawn`
registers tasks against the current nursery in
thread-local state; `NurseryEnd` semantics live
inside `_zo_nursery_join` — to be added in
Phase 6.

This keeps the codegen layer oblivious to
cancellation mechanics; every concurrency primitive
is a single `BL`.

### arg marshaling scoped to MVP.

Args already land in X0..X7 via the executor's
value lowering plus regalloc. For Phase 5, the
precise runtime ABI (e.g., `_zo_chan_new` taking
`ElemSize`, `Capacity` as distinct u64s) hasn't
been frozen — Phase 6 decides the struct layout.
The codegen emits the `BL` + caller-save
preamble; specific arg-slot massaging (e.g.,
splitting a tuple across X0/X1) lands when the
runtime signatures crystallize.

### new pub accessor.

`ARM64Gen::extern_used(&self) -> &[String]` —
exposes the accumulated extern symbol list for
tests to verify which runtime calls a program
pulled in. Same borrow pattern as other
`&[T]`-returning accessors in the crate.

### tests added.

Three tests in `zo-codegen-arm/src/tests/concurrency.rs`,
each compiling a program end-to-end through
tokenize → parse → execute → codegen and
asserting `extern_used` contents:

- `channel_create_registers_chan_new_extern` —
  a program using `channel()` pulls in
  `_zo_chan_new`.
- `task_spawn_registers_task_spawn_extern` —
  `spawn worker()` pulls in `_zo_task_spawn`.
- `nursery_markers_do_not_emit_externs` —
  a bare `nursery {}` pulls in zero
  `_zo_chan_*` / `_zo_task_*` symbols. Guards
  against accidentally lowering the markers to
  runtime calls.

### files touched.

| file | change |
|---|---|
| `zo-codegen-arm/src/codegen.rs` | +5 BL-emitting arms + no-op nursery arms before `_ => {}` fallback; +`pub fn extern_used` accessor |
| `zo-codegen-arm/src/tests.rs` | +`concurrency` module |
| `zo-codegen-arm/src/tests/concurrency.rs` | +3 tests via a shared `compile_and_inspect` helper |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-codegen-arm` | 37 / 37 (+3 vs pre-Phase-5) |
| `just zo_test` | 940 / 940 (10 skipped, +3 vs Phase 4) |

Zero behavioral change for non-concurrency
programs — the new arms only fire on SIR that
already contains the matching insns, and the
wildcard `_ => {}` fallthrough was previously
absorbing them silently.

### CLIF backend — gap noted.

The CLIF backend (`zo-codegen-clif/src/translate.rs`)
has a wildcard at its main match too. It
currently absorbs the concurrency insns without
emitting anything, so programs using channels
won't crash under CLIF but won't run correctly
either. Parallel Phase 5 work for CLIF is a
follow-up — not a blocker for the ARM-first
MVP path the plan targets.

### next — phase 6.

`zo-runtime/src/channel.rs` + `task.rs` —
`#[no_mangle] extern "C"` exports of
`_zo_chan_new`, `_zo_chan_send`, `_zo_chan_recv`,
`_zo_task_spawn`, `_zo_task_await`, plus the
nursery-join + cooperative-cancellation glue.
`zo-runtime/Cargo.toml` adds
`crate-type = ["lib", "staticlib"]` so the
build produces `libzo_runtime.a` for Phase 7
linking. Plan estimate ~200 LoC — first phase
since Phase 4 that touches a crate outside the
compiler proper.

---

## channels plan — Phase 6: runtime.

Per `PLAN_CHANNELS.md` Phase 6. The five
`#[no_mangle]` entry points plus supporting
state now live in `zo-runtime/src/channel.rs`
and `src/task.rs`. `cargo build -p zo-runtime`
produces `target/debug/libzo_runtime.a` —
Phase 7's link target.

### Cargo.toml — `staticlib` added.

`crate-type = ["lib", "staticlib"]` so the
workspace keeps using `zo-runtime` as a
regular Rust library (for the existing UI
dispatcher) while also producing a static
archive the Mach-O writer can consume. No
other crate rebuilds are required —
staticlib is purely additive at the artifact
layer.

### `channel.rs` — mutex + condvar FIFO.

```rust
pub struct ZoChan {
  inner: Mutex<ChannelInner>,
  senders: Condvar,
  receivers: Condvar,
  elem_sz: usize,
  capacity: usize,
}

struct ChannelInner {
  queue: VecDeque<Vec<u8>>,
  closed: bool,
}
```

Four exports:

- `_zo_chan_new(elem_sz, capacity) -> *mut ZoChan`
  — `Box::into_raw` a fresh channel. Capacity 0
  is treated as "unbuffered rendezvous" by
  sharing the same queue-bound path with a
  min-one slot.
- `_zo_chan_send(chan, src: *const u8)` — locks,
  waits on `senders` while queue ≥ bound,
  `memcpy`s `elem_sz` bytes into a pushed
  buffer, notifies one receiver.
- `_zo_chan_recv(chan, dst: *mut u8)` — mirror:
  waits on `receivers` while queue is empty,
  pops, `memcpy`s out, notifies one sender.
- `_zo_chan_free(chan)` — reclaims via
  `Box::from_raw`. Not exposed through SIR yet
  (the executor emits `_zo_chan_new` but no
  drop); will be wired when channel lifetimes
  cross the executor/runtime boundary.

ABI choice — values cross as raw byte pointers
sized by `elem_sz`. No runtime type descriptor;
the compiler is the source of truth for layout.
Keeps the runtime trivially generic.

### `task.rs` — pthread per task, stored outcome.

```rust
pub struct ZoTask {
  join: Mutex<Option<JoinHandle<()>>>,
  outcome: Arc<Mutex<TaskOutcome>>,
}

enum TaskOutcome { Running, Completed, Panicked }
```

Two exports, both `extern "C-unwind"` (see
below):

- `_zo_task_spawn(callee: extern "C-unwind" fn())
  -> *mut ZoTask` — `std::thread::spawn` runs
  the callee inside `catch_unwind`; the outcome
  is stored in the shared `Arc<Mutex<_>>` before
  the worker thread exits.
- `_zo_task_await(task)` — drains the
  `JoinHandle`, pops the outcome, and re-raises
  on the awaiting thread when the task panicked.
  Matches Phase 0 decision 1: stored `Result` at
  task boundary, no DWARF unwinding across FFI.

### `extern "C-unwind"` — not `extern "C"`.

Hit during Phase 6 test authoring: a panicking
`extern "C" fn` unwinding out of the function
pointer triggered SIGABRT with "thread caused
non-unwinding panic." Rust's default `extern
"C"` ABI treats such unwinding as UB; the
process aborts before `catch_unwind` can run.

The fix is `extern "C-unwind"` (stable since
Rust 1.71) — same calling convention on ARM64,
but tells the compiler panics may legally
propagate through. Used on both runtime
entries and the callee function-pointer type.

Implication for the ARM codegen (Phase 5) —
the `BL _zo_task_spawn` instruction itself is
ABI-neutral, but Phase 7 linking must tolerate
`C-unwind` metadata when pulling symbols from
`libzo_runtime.a`. No codegen changes required
today.

### tests added.

Seven inline unit tests split across the two
files, exercising the runtime directly:

`channel.rs`:
- `send_recv_round_trips_a_u64` — buffered
  capacity-4, one round trip.
- `buffered_fifo_order_is_preserved` — four
  values preserve order.
- `unbuffered_rendezvous_matches_sender_to_receiver`
  — capacity-0 channel with a producer thread;
  recv unblocks the sender exactly when it's
  called.

`task.rs`:
- `spawn_await_runs_the_callee_once` — an
  atomic counter confirms the callee ran.
- `await_on_panicked_task_propagates` —
  `#[should_panic]` confirms the propagated
  panic reaches the awaiting thread.

### files touched.

| file | change |
|---|---|
| `zo-runtime/Cargo.toml` | +`crate-type = ["lib", "staticlib"]` |
| `zo-runtime/src/lib.rs` | +`pub mod channel; pub mod task;` |
| `zo-runtime/src/channel.rs` | new — `ZoChan` + 4 exports + 3 tests |
| `zo-runtime/src/task.rs` | new — `ZoTask` + 2 exports + 2 tests |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `cargo build -p zo-runtime` | produces `target/debug/libzo_runtime.a` |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-runtime` | 7 / 7 (+5 vs pre-Phase-6) |
| `just zo_test` | 945 / 945 (10 skipped, +5 vs Phase 5) |

Zero behavioral change elsewhere — the runtime
is still loaded by `zo-driver` as a regular
library, the UI dispatcher is untouched.

### deferred from Phase 6 scope.

Four items intentionally left for later:

- **Value return-carry.** `TaskOutcome::Completed`
  is a unit variant; the task's actual return
  value isn't threaded back through `_zo_task_await`
  yet. Unblocked by the Phase 4b destructure
  work (consumers need to know the return type
  anyway).
- **Panic payload carry-through.** `Panicked` is
  a unit variant; the `Box<dyn Any>` payload is
  discarded. Cheap follow-up once a zo program
  exercises it.
- **Nursery thread-local state + cooperative
  cancellation flag.** Phase 0 decision 6 calls
  for a polled `nursery.cancelled` at
  `send`/`recv`/`await` entries. Needed once
  more than one task gets spawned into the
  same nursery.
- **`_zo_nursery_join`.** The codegen's
  `NurseryEnd` currently emits no code; the
  matching runtime hook will land when the
  join semantics diverge from raw pthread_join
  (e.g. cancel-all-on-sibling-panic).

### next — phase 7.

Mach-O writer links `libzo_runtime.a`:

- Read the archive, pull the `.text` +
  relocation entries for each referenced
  `_zo_chan_*` / `_zo_task_*` symbol.
- Resolve the existing `extern_fixups` from
  Phase 5's BL placeholders against the runtime
  symbol offsets in the linked binary.
- Mach-O header bookkeeping: bump section
  counts, append symtab entries, update
  LC_SYMTAB.

Plan estimate ~100 LoC. This is the first phase
to touch the Mach-O writer since CL17's ULEB128
consolidation — same crate, different concern.

---

## channels plan — Phase 7: dyld-linked runtime dylib.

Per `PLAN_CHANNELS.md` Phase 7. The original
plan called for static linking `libzo_runtime.a`
into the Mach-O binary — parsing the archive,
merging sections, resolving relocations. That's
real linker work (~500 LoC, Mach-O archive
parsing + per-relocation arithmetic) and isn't
warranted for an MVP where the runtime is still
evolving its ABI.

**Pivot**: route concurrency symbols through a
second `LC_LOAD_DYLIB` instead. dyld resolves
`_zo_chan_*` / `_zo_task_*` against
`libzo_runtime.dylib` at load time, same
mechanism as `libSystem` resolves `_malloc`.
~100 LoC touching two crates — cleanly in the
band the plan anticipated.

### changes.

**`zo-runtime/Cargo.toml`** — `crate-type` gains
`cdylib` alongside `lib` and `staticlib`:

```toml
crate-type = ["lib", "staticlib", "cdylib"]
```

`cargo build -p zo-runtime` now produces
`target/debug/libzo_runtime.dylib` in addition
to the `.a` and `.rlib` outputs.

**`zo-writer-macho/src/macho.rs`** — extended
`build_bind_opcodes` to take a per-entry dylib
ordinal:

```rust
// before:
pub fn build_bind_opcodes(
  entries: &[(&str, u8, u64)],      // (name, segment, offset)
  dylib_ordinal: u8,
) -> Vec<u8>

// after:
pub fn build_bind_opcodes(
  entries: &[(&str, u8, u64, u8)],  // (..., ordinal)
) -> Vec<u8>
```

Each entry now carries its own ordinal so one
opcode stream can resolve against multiple
dylibs. `BIND_OPCODE_SET_DYLIB_ORDINAL_IMM` is
emitted per-entry (previously redundant when
all entries shared an ordinal; free for the
multi-dylib case).

**`zo-codegen-arm/src/codegen.rs`** — three
coordinated edits:

1. New constants:
   ```rust
   const ZO_RUNTIME_DYLIB_ORDINAL: u8 = 2;
   const ZO_RUNTIME_SYMBOL_PREFIX: &str = "_zo_";
   ```
2. `generate_macho` partitions `extern_used` by
   symbol prefix. Symbols starting with `_zo_`
   get ordinal 2; everything else stays on
   libSystem (ordinal 1). A local flag tracks
   whether any symbol needed the runtime dylib.
3. `add_dylib("@executable_path/libzo_runtime.dylib")`
   fires after the libSystem entry, but only
   when `needs_runtime_dylib`. Non-concurrency
   programs get a single `LC_LOAD_DYLIB` as
   before.

`add_undefined_symbol` also picks the right
ordinal per symbol so the Mach-O symtab matches
the bind opcode stream.

### distribution model.

`@executable_path/libzo_runtime.dylib` — dyld
looks for the runtime next to the binary. MVP
runs require:

```sh
cp target/debug/libzo_runtime.dylib ./
./my_program
```

Post-MVP options: install to `/usr/local/lib`,
ship alongside the compiler's `target/release/`,
or use `@rpath` plus an `LC_RPATH` entry. None
change the Phase 7 code path.

### tests added.

Two tests in `zo-codegen-arm/src/tests/concurrency.rs`,
plus a new `compile_macho_and_inspect` helper that
runs the full pipeline through `generate_macho`
and hands the raw binary bytes to a predicate:

- `concurrency_binary_loads_runtime_dylib` —
  `nursery { channel() }` program contains both
  `"@executable_path/libzo_runtime.dylib"` and
  `"/usr/lib/libSystem.B.dylib"` substrings.
- `non_concurrency_binary_omits_runtime_dylib`
  — a plain `imu x = 42;` program contains the
  libSystem string but **not** any
  `"libzo_runtime.dylib"` substring.

Substring matching is deliberate — parsing
Mach-O load commands from the test would
duplicate the writer's work; the path string
is load-command-table payload, so its
presence/absence is a reliable signal.

### files touched.

| file | change |
|---|---|
| `zo-runtime/Cargo.toml` | +`cdylib` in `crate-type` (produces `libzo_runtime.dylib`) |
| `zo-writer-macho/src/macho.rs` | `build_bind_opcodes` takes per-entry ordinal |
| `zo-codegen-arm/src/codegen.rs` | +2 constants; partition extern_used by prefix; conditional `add_dylib` for runtime; undef-symbol ordinal routing |
| `zo-codegen-arm/src/tests/concurrency.rs` | +`compile_macho_and_inspect` helper + 2 tests |

### verification.

| gate | result |
|---|---|
| `cargo check --workspace` | clean |
| `cargo build -p zo-runtime` | produces `libzo_runtime.a` + `libzo_runtime.dylib` |
| `just typos` | clean |
| `just fmt` | clean |
| `just clippy` | zero warnings |
| `just test_crate zo-codegen-arm` | 39 / 39 (+2 vs pre-Phase-7) |
| `just zo_test` | 947 / 947 (10 skipped, +2 vs Phase 6) |

Zero behavioral change for non-concurrency
programs — `needs_runtime_dylib` stays false, no
new `LC_LOAD_DYLIB` is appended, and the bind
opcode stream only references libSystem as
before.

### deferred from Phase 7 scope.

- **Actual static link of `libzo_runtime.a`.**
  The staticlib still builds (kept in
  Cargo.toml's `crate-type`) for a future path
  that wants a single-file executable. Real
  static linking needs Mach-O archive parsing +
  relocation arithmetic; deferred until the
  runtime ABI stabilizes.
- **Automated dylib colocation in `zo run`.**
  `zo-driver` currently produces the binary and
  leaves it to the user to copy the runtime
  next to it. A follow-up can have the driver
  copy the dylib into the same directory as
  the output binary after a successful build.
- **`LC_RPATH` entry for `@rpath` resolution.**
  `@executable_path` is the simplest form;
  `@rpath` is more flexible for multi-binary
  installations but needs an extra LC_RPATH
  load command, touching the Mach-O writer's
  load-command bookkeeping.
- **CLIF backend parity.** CLIF's translator
  still absorbs concurrency insns via its
  wildcard — same gap Phase 5 noted. Will need
  its own `@executable_path` dylib wiring once
  CLIF codegen arms land.

### next — phase 8.

Tests. Plan calls for:
- `nursery-basic.zo` — nursery scope compiles.
- `spawn-basic.zo` — spawn + await.
- `channel-basic.zo` — send + receive.
- `channel-buffered.zo` — buffered channel.
- `channel-typed.zo` — type mismatch on send.
- `nursery-nested.zo` — nested nurseries.

Requires Phase 4b's destructure fix to land
first (so `imu (tx, rx) := channel()` binds
with `Ty::Channel(T)` not `Ty::unit`), plus a
convenience step in `zo run` to colocate the
dylib. After that, end-to-end runtime-backed
programs become executable for the first time.

---

## score card (CL18).

| item | kind |
|---|---|
| Phase 0 semantic decisions on PLAN_CHANNELS.md | design |
| `Ty::Channel(TyId)` + `Ty::Task(TyId)` variants | feat |
| `channel_type` / `task_type` constructors | feat |
| Channel / Task unification arms | feat |
| 7 smoke tests on the new ty layer | test |
| `handle_nursery_keyword` / `handle_spawn_keyword` | feat |
| Await routed to unary-stash path | feat |
| Nursery + Spawn closing cascades | feat |
| 6 parser tests pinning tree-shape contracts | test |
| 7 SIR `Insn` variants for channels / tasks / nursery | feat |
| SIR visitor arms + validator arms + pretty-printer | feat |
| 3 validator tests pinning placeholder + value-type coverage | test |
| Executor `nursery_stack` + `pending_spawn` state | feat |
| `execute_nursery` / `execute_spawn` / `execute_await` | feat |
| `execute_channel_builtin` + literal-capacity check | feat |
| `execute_channel_send` / `execute_channel_recv` | feat |
| Spawn → TaskSpawn redirect at Call emit | feat |
| 3 new `ErrorKind` variants (SpawnOutsideNursery, AwaitOnNonTask, ChannelCapacityNotLiteral) | feat |
| 6 executor tests on concurrency dispatch | test |
| Phase 4b known-gap documented (destructure types) | docs |
| ARM codegen arms for `Channel*` / `Task*` insns + BL to runtime syms | feat |
| Nursery markers lower to no-op (semantic only) | feat |
| `ARM64Gen::extern_used` pub accessor | infra |
| 3 ARM codegen tests pinning runtime-call emission | test |
| CLIF backend concurrency-insn gap noted | docs |
| `zo-runtime` staticlib target + `channel` / `task` modules | feat |
| 4 channel exports + 2 task exports (`#[no_mangle] extern "C-unwind"`) | feat |
| Stored-outcome panic propagation per Phase 0 decision 1 | feat |
| 5 runtime unit tests (send/recv roundtrip, rendezvous, spawn/await panic) | test |
| `libzo_runtime.a` produced, ready for Phase 7 linking | infra |
| `cdylib` added to `zo-runtime/Cargo.toml` | infra |
| `build_bind_opcodes` — per-entry dylib ordinal | refactor |
| `@executable_path/libzo_runtime.dylib` routing for `_zo_*` symbols | feat |
| 2 Phase 7 tests — `LC_LOAD_DYLIB` presence/absence | test |
| Pivot from static link to dyld-resolved dylib recorded | design |
