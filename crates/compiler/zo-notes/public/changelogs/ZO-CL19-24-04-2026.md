# ZO-CL19 — 2026-04-24

## PLAN_CONCURRENCY_CODEGEN follow-up.

CL18 landed the PLAN_PREHISTORY runtime + the first
end-to-end spawn + nursery path. CL19 closes the gap
between SIR-level concurrency and a compiled binary
that actually runs channels: producer / consumer
programs compile through `zo build` and execute
correctly from the binary.

`PLAN_CONCURRENCY_CODEGEN.md` lays out 4 phases.
Phases 1 and 2 land in this CL.

---

## Phase 1 — channel primitives end-to-end.

A program that `channel()`s, spawns a producer that
`.send(v)`s, spawns a consumer that `.recv()`s, and
prints the received value now runs from the compiled
binary.

### what lands

- `size_of_ty(TyId) -> u32` helper in codegen —
  resolves any TyId to its ABI byte width (int=4,
  bool=1, pointer-backed=8, etc.) from the
  tychecker's canonical registration order.
- `ChannelCreate` — single-dst refactor. Old
  `{ tx, rx, ... }` produced two aliased ValueIds
  but reg-alloc tracks one dst per insn, so `rx`
  had no register and `TupleLiteral` wrote
  garbage. Now `{ dst, ... }`; the executor's
  subsequent `TupleLiteral` aliases the one
  handle into the `(Tx<T>, Rx<T>)` pair.
- Codegen channel arg marshaling:
  - `ChannelCreate`: X0 = elem_sz, X1 = capacity,
    `BL _zo_chan_new`.
  - `ChannelSend`: value spilled to a 16-byte
    per-function scratch slot; X0 = chan,
    X1 = scratch address, `BL _zo_chan_send`.
  - `ChannelRecv`: runtime writes to scratch slot,
    codegen loads the result back into the dst
    register.
- `FunctionInfo::chan_scratch_size` + matching
  prologue / epilogue math in codegen reserve the
  16-byte scratch slot in the frame of any function
  using channels.
- `has_calls` recognition extended to cover every
  concurrency insn that emits a `BL`
  (`ChannelCreate / Send / Recv`, `TaskSpawn`,
  `TaskAwait`, `NurseryEnd`, `SelectWait`).
  Without this, concurrency-using functions got
  leaf prologues — caller-save STRs clobbered the
  return address and SIGBUS'd on exit.
- Codegen frame epilogue includes
  `chan_scratch_size` alongside the other frame
  components so SP restores correctly.
- N-ary `TaskSpawn` — runtime exports
  `_zo_task_spawn_1 / _2 / _3` plus matching
  `task_shim_1 / 2 / 3` that unpack
  `user_arg0..2` from the `ZoTask` before jumping
  to `user_entry`. Codegen selects the variant
  from `args.len()`; callee address lands in X0,
  args in X1..X3.
- Liveness `insn_uses` + `compute_value_ids`
  enumerate every concurrency operand and dst so
  the defining `TupleIndex` / `Load` / etc. survive
  DCE. Without this, `ch.0` loads were pruned and
  the runtime read garbage.
- DCE `is_impure` marks all concurrency insns as
  side-effecting. `collect_calls_in_range` treats
  `TaskSpawn.callee` as reachable so the spawned
  function survives DCE all the way to codegen's
  `function_addr_fixups`.
- SIR `emit()` returns `dst` for every concurrency
  value-producing insn (ChannelCreate, ChannelRecv,
  TaskSpawn, TaskAwait).

### test

- `concurrency-channel-roundtrip.zo` — two green
  tasks spawned under a nursery exchange `42`
  through a `channel()`, consumer prints `got 42`.
  Runs end-to-end in `just zo_test_runner`.

---

## Phase 2 — `tx.close()` / `rx.close()` surface.

Either half of a channel pair can close it; runtime
wakes every parked sender and receiver so they
observe the closed state on their next loop.

### what lands

- `Insn::ChannelClose { channel }` — single-operand,
  non-value-producing SIR insn.
- SIR visitor wiring: `visit_value_ids_mut` walks
  the channel operand; `visit_ty_ids_mut` no-op
  (no ty); `emit()` returns sentinel;
  `compute_value_ids` returns None.
- Liveness `insn_uses` reports the channel operand
  so the defining Load / TupleIndex stays alive.
- DCE `is_impure` marks `ChannelClose`.
- Validator — `ChannelClose` joins the no-ty-id
  arm.
- Pretty-printer — `chan.close %N`.
- Executor:
  - `is_dot_method_call` recognizes `close` on
    `ChannelTx` / `ChannelRx` so the Dot handler
    doesn't treat it as field access.
  - `execute_potential_call` routes `.close()` to
    new `execute_channel_close`, which pops the
    receiver from the three stacks and emits the
    `ChannelClose` insn.
- Codegen-arm — `Insn::ChannelClose` arm, X0 =
  channel handle, `BL _zo_chan_close`.
- Register-allocation `has_calls` and compiler's
  runtime-staging detection include `ChannelClose`.

### test

- `concurrency-channel-close.zo` — producer sends
  1 + 2 + closes; consumer receives both and
  prints `got 1` / `got 2`.

### deferred

`rx.recv_timeout(ms)` surface syntax. Needs
`Option<T>` wrapping (scope creep) or a bool-return
MVP that doesn't fit the current method-dispatch
shape without wider changes. The runtime primitive
(`_zo_chan_recv_timeout`) stays available; surface
syntax lands when a concrete use case demands it.

---

## Phase 3 — `select { arm, arm }` end-to-end.

`select { chan_expr => fn(p: T) => body, ... }` now
compiles to a binary that atomically waits on N
channels, routes control to the fired arm, and binds
the recv'd value to the closure parameter.

### what lands

- `Insn::SelectRecv { dst, which, ty_id, chans_len }`
  — companion to `SelectWait`. Split out so the
  allocator sees a single-dst insn per SIR entry;
  `SelectWait` produces `out_which`, `SelectRecv`
  produces the received value by LDRing the scratch
  buffer the runtime wrote into. `which` anchors the
  uses ⇒ the preceding `SelectWait` can't be
  reordered or DCE'd.
- `FunctionInfo::select_scratch_size` + matching
  prologue / epilogue math — `nchans * 8` bytes of
  `*mut ZoChan` pointers immediately followed by an
  `elem_sz` output buffer, sized at the worst-case
  select in the function and 16-byte aligned.
- Codegen `SelectWait` arm — spills each chan
  operand into the on-stack array, zeros the out
  buffer (so LDR of untouched upper bytes reads
  zero), loads X0 = chans_ptr, X1 = nchans,
  X2 = out_ptr, X3 = elem_sz, `BL _zo_select_wait`.
- Codegen `SelectRecv` arm — LDRs the received
  value from `select_scratch_base + chans_len * 8`
  into the allocator-assigned `dst` register.
- Executor `execute_select` rewrite — manually walks
  the select body (bypassing main-loop arm scan),
  splits arms by top-level `FatArrow` / `Comma`,
  evaluates each chan expression through
  `execute_node` to harvest its `ValueId`, emits
  `SelectWait` + `SelectRecv`, then per arm emits
  `ConstInt(i) + BinOp Eq + BranchIfNot + body +
  Jump(end) + Label(next)`. Derives `elem_ty` from
  the first `Rx<T>` / `Tx<T>` it meets.
- `execute_select_arm_body` — closure-arm form
  (`fn(p: T) => expr`) is the established surface;
  the executor extracts the param name from the
  `Fn` subtree, emits `Store { name, value: out_value,
  ty_id }` to allocate a frame slot mirror of the
  select's out buffer, and registers a `Local` so the
  body's `Ident(param)` emits a `Load` that lowers
  through codegen's `mutable_slots` table. A direct-
  expression fallback (no `fn` wrapper) is also
  accepted.
- `_zo_select_wait` runtime — when called from non-
  task code (e.g. `main` before the nursery drain)
  now drains one ready task per iteration via
  `scheduler::pop_local` + `run_one_external`.
  Without this, producers spawned inside `main`'s
  nursery never run before `select` polled them, and
  the select loop deadlocked on an empty queue.
- Liveness `compute_value_ids` / `insn_uses` wired
  for both `SelectWait` and `SelectRecv`. DCE
  `is_impure` marks both.
- SIR pp / validator cover the new variant.

### task return-value plumbing (unblocks
`concurrency-spawn-await` regression).

The test added in `f930fd62` was checked in without
the runtime half: `_zo_task_await` returned void and
the task's shims transmuted the user callee as
`fn()` (discarding the real X0 result). `await task`
therefore returned whatever junk sat in X0 after the
call, and `imu result = await task` bound to garbage.

Landed here:

- `ZoTask::ret_value: u64` — filled by the shim on
  the `Completed` transition, read by
  `_zo_task_await` before the `Box` reclaim.
- `task_shim / _1 / _2 / _3` transmute the callee as
  `fn(..) -> u64`; on arm64 reading X0 as `u64` is
  safe for every return width the ABI emits.
- `_zo_task_await(task) -> u64` — returns the
  captured value through X0; codegen's existing
  `emit_mov_reg(dst, X0)` after the `BL` stages it
  into the allocator-assigned `dst`.

### test

- `concurrency-select-pick.zo` — two channels, two
  producers, `select` picks the first to fire and
  prints `a: 10`. Runs end-to-end.

### score card (Phase 3).

| item | kind |
|---|---|
| `Insn::SelectRecv { dst, which, ty_id, chans_len }` | feat |
| `SelectWait` single-dst refactor (dropped `out_value`) | refactor |
| `FunctionInfo::select_scratch_size` + frame math | feat |
| `select_scratch_base` offset wired in codegen | feat |
| Codegen `SelectWait` arm — chans spill + ABI marshaling | feat |
| Codegen `SelectRecv` arm — LDR from out buf into dst | feat |
| Executor `execute_select` — arm walk + chan harvest + dispatch | feat |
| `execute_select_arm_body` — closure-param ↔ out_value binding | feat |
| `_zo_select_wait` drains run queue when called from non-task | fix |
| Liveness / DCE / pp / validator coverage for `SelectRecv` | feat |
| `ZoTask::ret_value` + shim `fn -> u64` transmute | fix |
| `_zo_task_await` returns `u64` (was void) | fix |
| `concurrency-select-pick.zo` — e2e select test | test |
| `concurrency-spawn-await.zo` — unblocked (was "got 1") | fix |
| 973 unit tests pass, 319 integration tests pass | test |

---

## Phase 4 — `t.cancel()` / `t.cancelled()` on `Task<T>`.

Cooperative cancellation surface. Mirrors the `Tx/Rx`
method shape — the handle carries the type, methods
attach to it.

### design note.

`PLAN_CONCURRENCY_CODEGEN.md` originally spec'd
`task::cancelled()` as a magic identifier with a
`_zo_task_current() -> *mut ZoTask` runtime hop. That
broke the established pattern (`.send/.recv/.close`
attach to `Tx<T>/Rx<T>`, not to a global `channel`
keyword). Reworked to `t.cancel()` / `t.cancelled()`
methods on `Task<T>` — consistent with every other
handle in the language, no reserved `task` keyword,
one fewer runtime call. Dropped the
`_zo_task_current` export entirely.

### what lands

- `Insn::TaskCancelled { dst, task, ty_id }` — reader.
  Returns `bool`. Lowered to `BL _zo_task_is_cancelled`
  with the task handle in X0.
- `Insn::TaskCancel { task }` — writer. No dst.
  Lowered to `BL _zo_task_cancel` with the handle in
  X0.
- `is_dot_method_call` recognizes `cancel` / `cancelled`
  on `Ty::Task(_)` so the Dot handler routes through
  the method-call path instead of field access.
- Executor `execute_task_cancel` / `execute_task_cancelled`
  pop the receiver from the three stacks and emit the
  matching insn (plus the bool result for
  `cancelled`). Dispatched from the postfix dot-call
  branch in `execute_potential_call` —
  `[recv, method, ., (, )]` — next to the existing
  `send` / `recv` cases.
- Liveness / DCE / pp / validator / codegen arms for
  both variants. Register allocator's `has_calls`
  detection includes both.
- Runtime: no new exports. Both
  `_zo_task_cancel(task)` and
  `_zo_task_is_cancelled(task)` were already present
  for internal use; Phase 4 just exposes them to the
  surface.

### test

- `concurrency-task-cancel.zo` — parent cancels a
  spawned worker, then reads back the flag. Prints
  `cancelled: true`.

### score card (Phase 4).

| item | kind |
|---|---|
| `Insn::TaskCancelled { dst, task, ty_id }` | feat |
| `Insn::TaskCancel { task }` | feat |
| `.cancelled()` / `.cancel()` method on `Task<T>` — surface | feat |
| `is_dot_method_call` recognizes Task methods | feat |
| Executor dispatch `execute_task_cancelled` / `execute_task_cancel` | feat |
| Codegen arms lower to `BL _zo_task_is_cancelled` / `_zo_task_cancel` | feat |
| Liveness / DCE / pp / validator / has_calls wiring | feat |
| Dropped spec'd `_zo_task_current` runtime export (no longer needed) | refactor |
| `concurrency-task-cancel.zo` — e2e | test |
| 973 unit tests pass, 320 integration tests pass | test |

---

## zo-runtime benches — criterion conversion.

The two `zo-runtime/benches/` files were hand-rolled
timing harnesses (`harness = false` + a bespoke
`main()` that printed wall-times). `zo-parser` and
`zo-executor` already run under `criterion`, so the
runtime was the odd one out. Converted both to
criterion:

- `runtime_bench.rs` — `fan_out / ping_pong /
  producer_consumer_close`. Driven by `Criterion::
  bench_with_input` + `Throughput::Elements` so
  criterion reports `Kelem/s` directly; correctness
  asserts on counters / sums stay intact.
- `cost_decomposition.rs` — stack alloc + zotask ctor
  + drop are plain `b.iter` closures; the two
  latency-sensitive benches (ctxsw roundtrip,
  spawn-to-entry) use `b.iter_custom(|iters| { ...;
  total })` so wall-times between reference points
  feed criterion's statistical harness instead of a
  hand-sorted `Vec<u64>`.
- `zo-runtime/Cargo.toml` — `criterion` as dev-dep
  (was missing); `harness = false` kept because
  `criterion_main!` provides its own `main`.

`just bench_runtime` is the consistent entry point
now. No new asserts; benches measure, they don't
fail on variance.

### score card (bench conversion).

| item | kind |
|---|---|
| `zo-runtime` benches migrated to `criterion` | refactor |
| `runtime_bench.rs` — `b.iter` + `Throughput::Elements` | refactor |
| `cost_decomposition.rs` — `b.iter_custom` for latency samples | refactor |
| `criterion` dev-dep added to `zo-runtime/Cargo.toml` | refactor |
| 973 unit tests pass (benches don't regress compile) | test |

---

## Growable task stacks — `mmap` + guard page + pool.

Replaced the eager 256 KiB `Box<[u8]>` per-task stack
with an 8 MiB virtual reservation, one-page initial
commit, and a process-wide SIGSEGV/SIGBUS handler that
extends the committed prefix on fault. Hot spawn/drop
reuses stacks through a bounded pool so the common
path skips `mmap` + `mprotect` entirely.

### success criteria (from `PLAN_GROWABLE_STACKS.md`)

| criterion               | baseline | target    | after      |
| ----------------------- | -------- | --------- | ---------- |
| `spawn_to_entry` median | 1.192 µs | < 300 ns  | **~42 ns** |
| `fan_out/4w/100000`     | 481 ms   | < 150 ms  | **79 ms**  |
| `fan_out/4w/10000`      | 48.3 ms  | —         | **8.9 ms** |

Both thresholds cleared by an order of magnitude. The
`spawn_to_entry` win comes entirely from the pool;
without it, the bare `mmap` path is roughly break-even
with the old `vec![0u8; 256*1024]` because macOS
syscall overhead dominates.

### what lands

- `zo-runtime/src/stack.rs` — new module. `TaskStack`
  owns (`base`, `top`, `low: AtomicPtr<u8>`). `reserve`
  pops from pool or `mmap` + `mprotect` on miss;
  `recycle` hands back to pool on drop. Registry is a
  `RwLock<Vec<*const TaskStack>>` read by the fault
  handler via spin-retry `try_read`.
- Signal handler covers SIGSEGV + SIGBUS with
  `SA_SIGINFO | SA_ONSTACK`, chains to the previous
  handler on unknown faults. Commits double on each
  growth up to the 8 MiB cap.
- `stack::ensure_sigaltstack` installs a 32 KiB
  alternate handler stack per pthread, called from
  `scheduler::run_one` before every ctx_switch into a
  task. Without this, a stack-overflow signal lands
  on the overflowing stack and double-faults
  immediately.
- Pool: `OnceLock<Mutex<Vec<Box<TaskStack>>>>`, cap 64.
  `reserve` / `recycle` hit the fast path; excess on
  drop falls through to `munmap`.
- `ZoTask` gains a `Drop` impl that moves its
  `_stack: Option<Box<TaskStack>>` into
  `TaskStack::recycle`, skipping the raw-munmap path.
- Diagnostic `FAULT_COUNT` / `EXTEND_COUNT` /
  `EXTEND_FAIL_COUNT` atomics + `stack::fault_stats()`
  pub getter — consumed by the Rust-side unit test to
  prove the handler actually fired on deep recursion.

### test

- `concurrency-task-stackgrow.zo` — zo program spawns
  a worker that recurses 2000 levels, returns the
  depth. Prints `depth: 2000`.
- `green_task_stack_grows_on_deep_recursion` —
  runtime-side unit test. Spawns a task that
  recurses through a 256-byte-frame function 1000
  times (~256 KiB of stack), asserts the runtime
  grew the committed region and no `mprotect`
  rejected the math.

### test-harness cleanup

Fixed pre-existing parallel-test isolation issues
across `zo-runtime`: tests sharing a module-level
`static COUNTER` would race when run under
`cargo test`'s default parallelism. Each test now
owns its own `static` counter + `extern "C-unwind"`
fn. 34/34 pass under parallel `cargo test`; 979/979
under nextest; 321/321 integration including the new
program.

### score card (growable stacks).

| item                                             | kind     |
| ------------------------------------------------ | -------- |
| `PLAN_GROWABLE_STACKS.md` — 4-phase plan         | plan     |
| `BASELINE_GROWABLE_STACKS.md` — pre-change nums  | plan     |
| `zo-runtime/src/stack.rs` — `TaskStack` + pool   | feat     |
| `mmap`/`mprotect` reserve + guard-page extend    | feat     |
| SIGSEGV + SIGBUS handler chained to previous     | feat     |
| `sigaltstack` per pthread in `scheduler::run_one`| fix      |
| Stack pool — `spawn_to_entry` 1.2 µs → 42 ns     | feat     |
| `ZoTask::_stack: Option<Box<TaskStack>>` + Drop  | feat     |
| `stack_alloc_256kb_vec0` bench deleted           | refactor |
| `task_stack_reserve_commit` bench added          | feat     |
| Per-test `static COUNTER`s across zo-runtime     | fix      |
| `concurrency-task-stackgrow.zo` — e2e            | test     |
| `green_task_stack_grows_on_deep_recursion`       | test     |
| `libc` workspace dep wired into zo-runtime       | feat     |
| 979 unit tests pass, 321 integration tests pass  | test     |

---

## score card (CL19).

| item | kind |
|---|---|
| `PLAN_CONCURRENCY_CODEGEN.md` — 4-phase plan, measurement-first | plan |
| `size_of_ty(TyId) -> u32` codegen helper | feat |
| `Insn::ChannelCreate { dst }` single-dst refactor | feat |
| `ChannelCreate` arg marshaling (X0=elem_sz, X1=capacity) | feat |
| `ChannelSend` / `ChannelRecv` scratch-slot marshaling | feat |
| `FunctionInfo::chan_scratch_size` + frame math | feat |
| `has_calls` recognizes every `BL`-emitting concurrency insn | fix |
| Frame epilogue includes `chan_scratch_size` | fix |
| `_zo_task_spawn_1 / _2 / _3` runtime exports | feat |
| `task_shim_1 / 2 / 3` arg-unpack shims | feat |
| Codegen N-ary spawn (callee in X0, args in X1..X3) | feat |
| Liveness `insn_uses` / `compute_value_ids` for concurrency insns | fix |
| DCE `is_impure` marks every concurrency insn | fix |
| DCE treats `TaskSpawn.callee` as reachable | fix |
| `SIR::emit()` returns dst for ChannelCreate/Recv, TaskSpawn/Await | fix |
| `concurrency-channel-roundtrip.zo` — end-to-end send/recv | test |
| `Insn::ChannelClose { channel }` SIR variant | feat |
| Full SIR / pp / validator / liveness / DCE wiring for ChannelClose | feat |
| Executor dispatch for `tx.close()` / `rx.close()` | feat |
| Codegen arm for `ChannelClose` (X0=chan, `BL _zo_chan_close`) | feat |
| `concurrency-channel-close.zo` — producer/consumer with close | test |
| 973 unit tests pass, 318 integration tests pass | test |
| `rx.recv_timeout(ms)` surface syntax deferred (runtime stays) | docs |
