-- fun main() {}

-- imu mul: fn(int, int): int = fn(x, y) -> x * y;
-- imu mil: fn(int, int): int = fn(x, y) { x * y };

-- mil(2, 4);

load "crates/compiler/zo-notes/samples/flow/flow.zo";

imu foo: int = x + 5;
