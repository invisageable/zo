-- arrays: declarations.

fun main() {
  imu x: [3]float = [1.0, 2.0, 3.0];   -- sized array.

  println(x); -- prints `[1.0, 2.0, 3.0]`.

  imu x: []float = [1.0, 2.0, 3.0];    -- dynamic array.

  println(x); -- prints `[1.0, 2.0, 3.0]`.
}
