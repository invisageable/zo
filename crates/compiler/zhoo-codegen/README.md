# zhoo-codegen.

> *The `code generation` phase.*

## about.

The `codegen` module produces the final bytecode. This module is responsible for translating the `ast` into executable code or target language, using a variety of optimizations to ensure optimal performance (`clif` and `llvm` only).   
