# zo-ast.

> *The `abstract syntax tree` of the `syntax analysis` phase.*

## about.

The current `ast` contains `expr` only. For the moment, everything in `zo` is an expression. New level should be added in the future but actually keep in mind that all instructions is an expression. Sounds good ?    

## dirmap.

```
```
