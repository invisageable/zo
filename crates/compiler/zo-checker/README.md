# zo-checker.

> *..*

## about.

Checkers are part of the `analyzing` compiler phase. Their role is to verify that the semantic still following the rules from the `zo` programming language. The checkers phase are:

- `name` — checks if the convention name is right. So if by inadvertence you're trying to use something not applicable by the `zo` programming languqge, an error will be handle, to let you correct it.      

## dirmap.

```
```
