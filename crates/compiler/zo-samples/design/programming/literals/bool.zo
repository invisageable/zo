-- literals: booleans.

fun main() {
  imu x: bool = true;
  imu y: bool = !x;
  imu z: bool = y == false;

  showln("x = {x}"); -- true.
  showln("y = {y}"); -- false.
  showln("z = {z}"); -- true.
}
