-- examples: GCD.

fun main() {
  -- recursive gcd of a and b in single line.
  imu gcd := fn(a, b) -> when b == 0 ? a : gcd(b, a % b);

  val A: int = 98;
  val B: int = 56;

  println("GCD of {a} and {b} = {}", gcd(A, B)); -- returns `GCD of 98 and 56 is 14`.
}
