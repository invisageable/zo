-- syntax overview.

-- this is a line comments.
-! this is a line doc comments.

val A: int = 0.0;
val B: int = 1.0;

fun main() {
  val C: int = 0.0;
  val D: int = 1.0;

  imu a: (int, float) = (40, 2.0);
  imu b: int = a.0;
  imu c := a.1;

  mut d: float[] = [1, 2, 3];
  mut e: int = d[2];
  mut f: Fn(int): int = fn(x) -> x * x * d[x];

  -- imu foo ::= <></>;

  f(2)
}
