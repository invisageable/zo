# zo-analyzer.

> *The `analyzing` phase.*

## about.

The `analyzer` performs semantic analysis on the `ast`, ensuring that the program is well-formed and complies with the language rules. It produces a fully-typed `ast`, which is then passed to the code generator module.    

## dirmap.

```
```
