# zhoo-builder.

> *The `building` phase.*

## about.

The bytecode is passed to the `builder` module, which generates the final executable code or target language code. This code can be output in a variety of formats, including `.exe`, `.js`, `.py` and `.wat|.wasm`.       
