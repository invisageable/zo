# zhoo-reader.

> *The `reading` phase — It reads the source code.*

## about.

The `reader` module takes the source code from a file or input stream and produces a stream of bytes that represent the input.

## dirmap.

```
benches                   # ...
src                       # ...
|-- lib.rs                # ...
|-- reader.rs             # ...
|-- test.rs               # ...
```
