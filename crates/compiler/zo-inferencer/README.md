# zo-inferencer.

> *The `inferencing` compiler phase*

## about.

The type inference implementation came from [typical](https://github.com/ravern/typical) by [ravern](https://github.com/ravern). I'll try to adapt his implementation for mine but actually I'm stuck. This phase is part of the `type checking` compiler phase. Its role is to deduce the different types from the ast without having to declare them upstream in the code.    

## dirmap.

```
```
