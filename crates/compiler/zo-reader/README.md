# zo-reader.

> *The `reading` phase — It reads the source code.*

## about.

The `reader` module takes the source code from a file or an input stream and produces a array of bytes that represent the input.

## dirmap.

```
src                       # ...
|-- lib.rs                # import all the files related by the `reader`.
|-- reader.rs             # the instance of the reader.
```
