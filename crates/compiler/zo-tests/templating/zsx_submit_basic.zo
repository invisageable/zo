-- test-build-pass: @submit event binds and receives the input's value payload.

load core::zsx::*;

fun main() {
  mut text: str = "";
  imu view: </> ::= <>
    <input
      value={text}
      @input={fn(e) => text = e.value}
      @submit={fn(e) => text = e.value}
    />
  </>;

  #dom view;
}
