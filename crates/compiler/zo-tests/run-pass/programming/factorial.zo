-- tests-run-pass: factorial.
-- @see https://en.wikipedia.org/wiki/Factorial.

fun fact(x: int) -> int {
  when x <= 1 ? 1 : x * fact(x - 1)
}

fun main() {
  check@eq(fact(5), 120);
  showln(fact(20));
}
