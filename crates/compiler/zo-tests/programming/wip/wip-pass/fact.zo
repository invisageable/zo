-- tests-build-pass: factorial (from rust-prehistory).
-- @cmd — zo run fact.zo

fun f(x: int) -> int {
  if x == 1 {
    return 1;
  } else {
    imu y: int = x * f(x - 1);

    return y;
  }
}

fun main() {
  check(f(5) == 120);
}
