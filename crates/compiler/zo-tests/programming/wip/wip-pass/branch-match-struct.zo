pub struct Rect {
  x: float,
  y: float,
  w: int,
  h: int,
}

fun main() {
  imu rect: Rect = Rect { x: 0.0, y: 14.0, w: 1080, h: 640 };

  match rect {
    Rect { x: 0.0, y, .. } => {
      check@eq(x, 0.0);
      showln(x);
    },
    Rect { x, y: 14.0, .. } => {
      check@eq(y, 14.0);
      showln(y);
    },
    Rect { x, y, w, h } => {      
      showln("x: {x}, y: {y}, w: {w}, h: {h}");
    },
  }
}
