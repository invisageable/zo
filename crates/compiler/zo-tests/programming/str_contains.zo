-! tests-run-pass: `str.contains` / `str.index_of`.
-!
-! @cmd — zo build str_contains.zo

fun main() {
  imu i: int = "hello world".index_of("world");
  showln(i);

  imu j: int = "hello".index_of("z");
  showln(j);

  imu c: bool = "abc".contains("b");
  showln(c);
}

-- EXPECTED OUTPUT:
-- 6
-- -1
-- true
