-- tests-run-pass: `for x := a..b => expr;` — single-
-- expression for-loop body via `=>` shortcut.

fun main() {
  -- for:line.
  for x := 0..3 => showln("{x}");

  -- for:line:mut.
  -- for mut n := 0..3 => n += 1;
}

-- EXPECTED OUTPUT:
-- 0
-- 1
-- 2
