fun main() {
  imu x: int = 10;

  fun f(n: int, outer: int) -> int {
    if n > 10 {
      return outer + 10;
    }

    return outer;
  }

  check@eq(f(37, x), 20);
  check@eq(f(5, x), 10);
  check@eq(x, 10);
}
