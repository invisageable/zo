-- tests-build-pass: ternary with expression condition.
-- @cmd — zo build branch-ternary-expr.zo

fun abs(x: int) -> int {
  when x >= 0 ? x : 0 - x
}

fun main() {
  check@eq(abs(5), 5);
  check@eq(abs(0), 0);
}
