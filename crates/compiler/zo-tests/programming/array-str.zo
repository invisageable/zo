-- tests-build-pass: string arrays.
-- @cmd — zo run array-str.zo

fun main() {
  imu a: []str = ["hello", "world", "zo"];

  showln(a[0]); -- expected: `hello`
  showln(a[1]); -- expected: `world`
  showln(a[2]); -- expected: `zo`
}
