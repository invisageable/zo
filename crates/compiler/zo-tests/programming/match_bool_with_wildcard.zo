-- tests-build-pass: bool match exhaustive via wildcard.
-- @cmd — zo build match_bool_with_wildcard.zo

fun main() {
  imu b: bool = true;

  match b {
    true => check(true),
    _    => check(false),
  }
}
