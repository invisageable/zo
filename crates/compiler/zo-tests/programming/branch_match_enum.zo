pub enum Exit {
  Yes(int),
  No(int)
}

fun main() {
  imu exit: Exit = Exit::Yes(0);

  match exit {
    Exit::Yes(code) => {
      check@eq(code, 0);
      showln(code);
    },
    Exit::No(code) => {
      showln(code);
      check(false);
    },
  }
}

-- EXPECTED OUTPUT:
-- 0
