-- tests-run-pass: all libm functions.
-- all ext functions are preloaded from std/math.zo.

fun main() {
  showln(pow(2.0, 10.0));
  showln(sin(0.0));
  showln(cos(0.0));
  showln(tan(0.0));
  showln(log(1.0));
  showln(log2(8.0));
  showln(log10(100.0));
  showln(exp(0.0));
}
