-- tests-run-fail: mismatched types in generic struct fields.
-- Both fields are $T, but int != str.

struct Pair<$T> {
  first: $T,
  second: $T,
}

fun main() {
  imu p := Pair { first: 1, second: "hello" };
}
