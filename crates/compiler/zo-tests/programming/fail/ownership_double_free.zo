-! Double-free: `Vec::free(own self)` consumes the receiver,
-! so the second `.free()` reads a moved value.
fun main() {
  mut v: Vec<int> = Vec::new();
  v.push(1);
  v.free();
  v.free();
}

-- EXPECTED ERROR: E0351
