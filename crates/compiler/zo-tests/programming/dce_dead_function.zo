-- tests-build-pass: dead function eliminated, program runs.
-- @cmd — zo run dce_dead_function.zo

fun dead_fn() -> int {
  return 42;
}

fun helper(x: int) -> int {
  return x * 2;
}

fun main() {
  imu a: int = helper(10);

  check(a == 20);
}
