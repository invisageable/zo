load std::math::*;

-- tests-build-pass: ternary with expression condition.
-- @cmd — zo build branch_ternary_expr.zo

fun abs(x: int) -> int {
  when x >= 0 ? x : -x
}

fun main() {
  check@eq(abs(5), 5);
  check@eq(abs(0), 0);
  check@eq(abs(-7), 7);
}
