-- tests-build-pass: enum construction with inferred type.
-- @cmd — zo build enum-construct-infer.zo

enum Color {
  Red,
  Green,
  Blue,
}

fun main() {
  imu r := Color::Red;
  imu g := Color::Green;
  imu b := Color::Blue;
}
