-- tests-build-pass: apply with Self type and struct return.
-- @cmd — zo build apply-self.zo

struct Span {
  lo: int,
  hi: int,
}

apply Span {
  fun zero() -> Self {
    Self { lo: 0, hi: 0 }
  }
}

fun main() {
  imu s := Span::zero();
}
