-- tests-build-pass: apply block on enum type.
-- @cmd — zo build apply-enum.zo

enum Color {
  Red,
  Green,
  Blue,
}

apply Color {
  fun red() -> Self {
    Self::Red
  }
}

fun main() {
  imu c: Color = Color::red();

  check@eq(c, Color::Red);
}
