-- core::bool inherent methods.

fun main() {
  imu is_true: bool = true;
  imu is_false: bool = false;

  check@eq(is_true.as_int(), 1);
  check@eq(is_false.as_int(), 0);

  showln(is_true.as_int());
  showln(is_false.as_int());

  showln("DONE");
}

-- EXPECTED OUTPUT:
-- 1
-- 0
-- DONE
