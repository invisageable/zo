-- tests-build-pass: basic ternary expression.
-- @cmd — cargo run --bin zo -- build crates/compiler/zo-tests/build-pass/programming/branch-ternary.zo -o crates/compiler/zo-tests/build-pass/programming/branch-ternary

fun main() {
  imu x: int = when true ? 1 : 2;

  check@eq(x, 1);
}
