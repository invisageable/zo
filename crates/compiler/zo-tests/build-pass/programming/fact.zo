-- tests-run-pass: factorial (from rust-prehistory).
-- @cmd — cargo run --bin zo -- run crates/compiler/zo-tests/build-pass/programming/fact.zo

fun f(x: int) -> int {
  if x == 1 {
    return 1;
  } else {
    imu y: int = x * f(x - 1);
    return y;
  }
}

fun main() {
  check(f(5) == 120);
}
