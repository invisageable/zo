-- tests-build-pass: generic apply block.
-- @cmd — zo build generic-apply.zo

struct Pair<$T> {
  first: $T,
  second: $T,
}

apply Pair<$T> {
  fun new(a: $T, b: $T) -> Self {
    Self { first: a, second: b }
  }
}

fun main() {}
