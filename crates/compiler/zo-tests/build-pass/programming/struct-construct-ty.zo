-- tests-build-pass: struct construction with explicit type annotation.
-- @cmd — zo build struct-construct-ty.zo

struct Span {
  lo: int,
  hi: int,
}

fun main() {
  imu s: Span = Span { lo: 0, hi: 10 };
}
