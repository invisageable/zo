-- tests-build-pass: enum tuple variant construction.
-- @cmd — zo build enum-construct-tuple.zo

enum Foo {
  Ok(int),
  Err(int),
}

fun main() {
  imu x := Foo::Ok(42);
  imu y: Foo = Foo::Err(1);
}
