-- tests-build-pass: early return from conditional.
-- @cmd — zo build early-return.zo

fun f() -> int {
  if true {
    return 1;
  }

  return 0;
}

fun main() {
  imu result: int = f();

  check(result == 1);
}
