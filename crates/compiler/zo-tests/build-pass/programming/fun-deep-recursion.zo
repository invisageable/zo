-- tests-build-pass: deep recursion (from rust-prehistory).
-- @cmd — zo build fun-deep-recursion.zo

fun f(x: int) -> int {
  if x == 1 {
    return 1;
  } else {
    imu y: int = 1 + f(x - 1);

    return y;
  }
}

fun main() {
  check(f(5000) == 5000);
}
