-- tests-build-pass: generic struct construction + field access.
-- @cmd — zo build generic-struct.zo

struct Pair<$T> {
  first: $T,
  second: $T,
}

fun main() {
  imu p := Pair { first: 10, second: 20 };

  showln(p.first);
  showln(p.second);
}
