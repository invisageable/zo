fun main() {
  -- for:block.
  for x := 0..3 {
    showln("{x}");
  }

  -- for:block:mut.
  -- for mut x := 0..3 {
  --   x += 1
  -- }

  -- for:line.
  for x := 0..3 => showln("{x}");

  -- for:line:mut.
  -- for mut n := 0..3 => n += 1;
}
