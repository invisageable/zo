# SWK-CL01 — 04-01-2026.

APi CHANGES:

**swisskit-renderer**

- `Webview` struct has been renamed `HtmlRenderer` located in [`@html` module](../../swisskit-renderer/src/html.rs).
- The `pdf` module has been re-structure:

```rust
pub mod data;
pub mod error;
pub mod platform;
pub mod renderer;

// PUBLiC APi examples — `use swisskit_render::<module_name>::*;`
```
