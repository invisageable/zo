use eazy::Curve;
use eazy::easing::logarithmic::log10::InLog10;
use eazy::interpolation::linear::lerp::Lerp;

fn main() {
  // for x in (0.01..=1.0).step_by(10.0) {
  // let log_weight = InLog10.y(x);
  // let confidence = Lerp.y(log_weight);

  // println!("input: {x:.2}, confidence: {confidence:.4}");
  // }
}
